# Base URL of the backend API, including the /api prefix.
# Local development:  http://localhost:5000/api
# Production (Vercel): set this to your deployed Render backend's URL, e.g.
#   https://your-backend.onrender.com/api
VITE_API_URL=
